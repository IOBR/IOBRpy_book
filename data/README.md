# Demo data

`demo_samples.csv` records all six public paired-end RNA-seq runs from the
colorectal cancer spheroid project `PRJDB16684`.

The FASTQ source URLs and checksums are recorded in `demo_samples.csv`.
